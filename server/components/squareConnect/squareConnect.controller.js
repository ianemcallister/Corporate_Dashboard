/*
*	This controlls all the interactions with squareConnect
*/

'use strict';

var https = require('https');

// Gets a list of transactions at a location between specific times
export function allTransactions(req, res) {
	return new Promise(function(resolve, reject) { resolve('test') });
//function allTransactions(req, res) {
  /*return new Promise(function(resolve, reject) {
  	resolve('test');
  })
  .then()
  .catch();*/
  /*return Thing.find().exec()
    .then(respondWithResult(res))
    .catch(handleError(res));*/
}

//get a list of all locations --might not need this
